/* $Id$ */

/**
 *  @since  February 2009
 */

#include <soc/interface.h>

    .section .init.start

    BEGIN_PROC(kernel_arch_init)

    mov.l   init_memory, r0
    jmp     @r0
    nop

    END_PROC(kernel_arch_init)
